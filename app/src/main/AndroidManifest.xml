<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.transpack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SignUpCustomer" />
        <activity android:name=".SignInCustomer" />
        <activity android:name=".MyVoyageList" />
        <activity android:name=".currentuser" />
        <activity android:name=".VoyageListss" />
        <activity android:name=".ADDmission" />
        <activity android:name=".ChooseOption" />
        <activity android:name=".activity_sign_up" />
        <activity android:name=".SignIn" />
        <activity android:name=".activity_shared" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>